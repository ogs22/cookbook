Prefix meaning a thousand million, i.e. $10^{9}.$ The symbol G is used,
for example one Gm is $10^{9}$ metres.

In computing contexts, when we are measuring a number of bytes, giga is
used to mean $2^{30}.$
