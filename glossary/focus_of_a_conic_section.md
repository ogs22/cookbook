If we define a conic section as the locus of a point such that its
distance from a given line is in constant ratio to its distance from a
given point, then the given point is called a focus of the conic
section.
