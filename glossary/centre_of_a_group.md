Given a group G, the set Z(G) of those elements g of G which commute
with every element of G:
$ \mathrm{Z(G)} = { \mathrm{g} \in \mathrm{G} : \; \mathrm{g.h=h.g}
\; \forall \mathrm{h} \in \mathrm{G} } $ - is a group. It is called the
centre of G.
