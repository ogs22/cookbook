Since E and E’ are disjoint, we know that
$ \mathrm{p(E} \cup \mathrm{E} ^{\prime} )
= \mathrm{p(E)+p(E} ^{\prime} {)}. $ But
$ \mathrm{E} \cup \mathrm{E} ^{\prime}  = \mathrm{S} $ and p(S)=1, so
p(E)+p(E’)=1 or p(E’)=1-p(E).
